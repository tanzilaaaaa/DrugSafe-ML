version: '3.8'

services:
  web:
    build: .
    ports:
      - "5001:5001"
    environment:
      - FLASK_ENV=production
      - FLASK_APP=src/main.py
    volumes:
      - ./models:/app/models
      - ./data:/app/data
    restart: unless-stopped
